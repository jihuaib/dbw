fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9
python-docx>=1.1          # Word 资料导入
httpx>=0.27               # OpenAI 兼容端点（DeepSeek / GLM / 通义等）
paramiko>=3.4             # SSH 设备接入（Telnet 用原生 socket，无需依赖）
anthropic>=0.40           # Claude；未配置 Key 时自动走 F4 兜底
pytest
langgraph
langchain-openai
langchain-anthropic
langchain-community
pysmi==0.3.4
pysnmp>=7.1,<8      # asyncio 版，Python 3.9~3.13 通用（4.4 依赖已移除的 asyncore/imp）
pyasn1>=0.4.8,<0.7
